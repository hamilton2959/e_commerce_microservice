spring:
  application:
    name: config-server
server:
  port: 8888
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations